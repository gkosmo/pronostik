<br>
<br>
<% if question.scenarios.any? %>
  <div class="btn-group">
    <% question.scenarios.each do |scenario| %>
      <button type="button" class="btn btn-default">
        <%= scenario.content%>
      </button>
    <% end %>
  </div>
<% end %>
